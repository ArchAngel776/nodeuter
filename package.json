{
  "name": "nodeuter",
  "version": "0.1.0",
  "description": "A compact, elegant TypeScript backend foundation for Node.js.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && yarn build:js && yarn build:types",
    "build:js": "babel src --out-dir dist --extensions .ts --source-maps",
    "build:types": "tsc --emitDeclarationOnly && tsc-alias -p tsconfig.json",
    "dev": "yarn build && NODE_ENV=development node --watch dist/index.js",
    "prod": "yarn build && NODE_ENV=production node dist/index.js",
    "test:all": "yarn build && NODE_ENV=test TS_NODE_TRANSPILE_ONLY=true TS_NODE_COMPILER_OPTIONS='{\"module\":\"commonjs\"}' node --require ts-node/register --test $(find tests -type f -name '*.test.ts')",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "packageManager": "yarn@4.9.2",
  "devDependencies": {
    "@babel/cli": "^7.28.6",
    "@babel/core": "^7.29.0",
    "@babel/plugin-proposal-decorators": "^7.29.0",
    "@babel/plugin-transform-class-properties": "^7.28.6",
    "@babel/plugin-transform-class-static-block": "^7.28.6",
    "@babel/plugin-transform-classes": "^7.28.6",
    "@babel/preset-env": "^7.29.2",
    "@babel/preset-typescript": "^7.28.5",
    "@types/node": "^25.6.0",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "babel-plugin-typescript-path-aliases": "^1.1.3",
    "eslint": "^8.57.1",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.17",
    "typescript": "^6.0.3"
  }
}
